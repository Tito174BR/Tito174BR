# Opcional. O GitHub Actions injeta GITHUB_TOKEN automaticamente.
GITHUB_TOKEN=

# Opcional para aumentar o limite da API pública do GitLab.
GITLAB_TOKEN=

# Apenas para o pipeline do GitLab fazer commit automático.
# Crie um Project Access Token com write_repository.
GITLAB_PUSH_TOKEN=
